from setuptools import setup

setup(name='json2tex',
      version='0.1',
      description='A module to convert 5e compatible json to LaTeX (and PDF)',
      author='The_Reto',
      packages=['json2tex'])